

<link type="text/css" rel="stylesheet" href="~/Content/Header.css?Wednesday 20th of July 2016 07:45:21 AM" />
    <script src='@Url.Content("~/Scripts/jquery-3.1.0.min.js")' type="text/javascript"></script>
    <script src='@Url.Content("~/Scripts/jquery-ui-1.11.4.min.js")' type="text/javascript"></script>
    <div id="Header">
        <ul id="HeaderList">
            <li class="listForHead">  
                <a class="menuItems" href="http://localhost:18157/MainPage/Index">
                    <h3 class="MenuNavItem">Main</h3>
                </a>       
            </li>
            <li class="listForHead">
                <h3  class="MenuNavItem">
                    @Html.ActionLink("Methods of delivery", "MethodsOfDelivery", "Menu")
                </h3>
            </li>
            <li class="listForHead">
                <h3  class="MenuNavItem">
                    @Html.ActionLink("About Shop","AboutShop","Menu")
                </h3>
            </li>
            <li class="listForHead">              
                    <h3  class="MenuNavItem">
                         @Html.RouteLink("Contacts", "MenuRoute")
                    </h3>            
            </li>
            <li  class="listForHead">
                @if (!Request.IsAuthenticated)
                {
                    <div id="SignInMenu">
                        <h4 class="MenuNavItem">
                            @Html.ActionLink("Sign in", "SignIn", "Auth",
new { returnUrl = Request.RawUrl }, new { })
                        </h4>
                        <h4 class="MenuNavItem" style="margin-left:6px">
                            @Html.ActionLink("Sign up", "SignUp", "Auth",
new { returnUrl = Request.RawUrl }, new { })
                        </h4>
                        <div id="AuthPartWin">
                            @*<form action="/Auth/SignIn" method="get">
                                <button id="signInHead">Sign in</button>
                            </form>*@
                            @using (Html.BeginForm(
                                "startSignIn", "Auth", new { returnUrl = Request.RawUrl }, FormMethod.Post))
                            {
                                <input id="signInHead" type="submit" value="Sign in"/>
                            }                            
                           
                            <div id="authSectionDevider">
                                <span style="display:inline-block">or</span>
                            </div>                           
                            <p>
                                Sign up to be able to buy products from shop
                                @Html.ActionLink("Create accaunt", "SignUp", "Auth",
new { returnUrl = Request.RawUrl }, new { })
                            </p>   
                                
                        </div>
                    </div>
                }
                else
                {
                    <h4 class="MenuNavItem">
                        @Html.ActionLink(User.Identity.Name, "showUserProfile", "Auth",
               new { returnUrl = Request.RawUrl }, new { })                   
                    </h4>
                    <h4 class="MenuNavItem">
                        @Html.ActionLink("[logout]", "SignOut", "Auth",
               new { returnUrl = Request.RawUrl }, new { })
                    </h4>
                }
            </li>

        </ul>
    </div>

